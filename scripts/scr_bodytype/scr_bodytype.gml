
/// @description How a physics body interacts with other objects.
enum BodyType {
	/// Object moves when pushed. Object can push other objects.
	DYNAMIC,
	/// Object does not move when pushed. Object can push other objects.
	KINEMATIC
}